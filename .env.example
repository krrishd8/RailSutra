# Database Connection (SQLite local file)
DATABASE_URL="file:./dev.db"

# Optional Gemini API Key for AI Operations Assistant (Fallback rule-engine used if omitted)
GEMINI_API_KEY=""

# Application Environment
NODE_ENV="development"
